package vl

// find actions

// action
// rabbit mac, schedule expression, action, actionparams
// valid expression : every x minutes/hours, at 7am every days/weekdays/monday...

// filter actions that needs to be played now

// if match, create packet, put it in queue

// done

